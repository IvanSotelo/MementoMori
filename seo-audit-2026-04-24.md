# SEO Audit Report — memento-mori.mx

**Respetable Logia Simbólica Memento Mori N.° 107 | Chihuahua, México**
**Audit Date:** April 24, 2026 | **Last updated:** April 29, 2026 | **Stack:** Nuxt 3 · Netlify · Bilingual ES/EN

---

## Estado general

| Score inicial | Score proyectado tras fixes | Fixes implementados |
| ------------- | --------------------------- | ------------------- |
| 42 / 100      | ~68 / 100                   | 19 de 29 issues     |

| Category                  | Weight | Score inicial | Score actual |
| ------------------------- | ------ | ------------- | ------------ |
| Technical SEO             | 22%    | 51/100        | ~78/100      |
| Content Quality (E-E-A-T) | 23%    | 44/100        | ~50/100      |
| On-Page SEO               | 20%    | 38/100        | 68/100       |
| Schema / Structured Data  | 10%    | 20/100        | 65/100       |
| Performance (CWV)         | 10%    | 52/100        | ~72/100      |
| AI Search Readiness (GEO) | 10%    | 31/100        | 52/100       |
| Images                    | 5%     | 35/100        | 38/100       |

**Supplementary:** Local SEO Score: **28 / 100** (sin cambios — requiere GBP y dirección física)

---

## Leyenda

- ✅ **Implementado** — commit aplicado
- ⏳ **Pendiente de contenido** — requiere texto/datos del equipo
- 🔲 **Pendiente de acción externa** — requiere acción fuera del codebase

---

## Issues Críticos

### C-1. `robots.txt` returns 404 ✅

`public/_robots.txt` renombrado a `public/robots.txt`. Googlebot y crawlers de IA ahora reciben correctamente las directivas de crawl y la declaración del sitemap.

### C-2. `sitemap.xml` returns 404 ✅

Creado `public/sitemap.xml` con las 14 URLs del sitio (7 ES + 7 EN) con anotaciones `xhtml:link` hreflang en cada entrada y fechas `lastmod` reales del historial de git.

### C-3 + C-4. `lang="es"` en páginas EN / Zero hreflang tags ✅

Añadidos campos `language: 'es-MX'` y `language: 'en-US'` a los locales en `nuxt.config.ts`. Esto desencadenó automáticamente:

- `<html lang>` reactivo al locale activo
- Generación automática de `<link rel="alternate" hreflang>` en todas las páginas
- `og:locale` reactivo (es_MX / en_US) en `app.vue`

### C-5. MP3 de 7.53 MB descargado al cargar la página ✅

El audio ya no se inicializa en `onMounted`. El `Howl` se crea solo cuando el usuario activa el toggle de sonido por primera vez. LCP móvil estimado: 53.5 s → ~5 s.

### C-6. Homepage tiene ~15 palabras de texto rastreable ✅

Añadido bloque visible `aside.page-home__intro` en `pages/index.vue` con ~300+ palabras por idioma (`home-intro` en `i18n/locales/es.json` y `en.json`): identidad, Gran Logia Cosmos, misión, valores en Chihuahua y propósito del sitio. Estilo compacto al pie del viewport (degradado + scroll) para no alterar el hero.

### C-7. Traducciones EN del código moral incomprensibles ⏳

Los ítems 7, 8, 11, 14 y 15 de `i18n/locales/en.json` tienen output de Google Translate incomprensible. Requieren traducción humana.

### C-8. `itemprop="breadcrumb"` Microdata inválido ✅

Eliminado `itemprop="breadcrumb"` de `AppHeader.vue`. Reemplazado por BreadcrumbList JSON-LD en cada página interior (ver M-BreadcrumbList).

---

## Issues High Priority

### H-1. No hay canonical tags ✅

Auto-generados por `@nuxtjs/i18n` al poblar los campos `language` en locales (cascada del fix C-3).

### H-2. Faltan meta descriptions en /history, /moral-code, /message, /gallery ✅

Añadido `useSeoMeta` con title y description reactivos al locale en las 4 páginas. También añadido en `/credits`.

### H-3. Organization schema solo en /ser-mason ✅

Añadido bloque `Organization` + `WebSite` JSON-LD en `pages/index.vue` con todos los campos requeridos: `@id`, `address`, `foundingDate`, `email`, `logo`, `contactPoint`, `parentOrganization`. El schema es reactivo al locale para la descripción.

### H-4. No hay Google Business Profile 🔲

Crear o reclamar listing en [business.google.com](https://business.google.com). Categoría: "Masonic lodge" / "Organización fraternal". Agregar dirección física y verificar. Sin GBP el lodge no puede aparecer en el Map Pack.

### H-5. No se publica ninguna dirección física ⏳

Requiere la dirección del salón de sesiones para agregar al footer (NAP), schema `PostalAddress`, y GBP.

### H-6. Sprite PNG de 2.15 MB + favicon de 152 KB ⏳

- `sprite_test5.png` necesita conversión a WebP (ahorro estimado ~1.3 MB) y `loading="lazy"`
- `favicon.png` necesita redimensionarse a 32×32 px (<5 KB)

### H-7. Imágenes de menú JPEG sin WebP ⚠️ revertido

Se intentó migrar a Netlify Image CDN pero el formato `%2F` causó 502 en producción. Revertido a URLs estáticas originales `/1.jpeg` … `/5.jpeg`. La optimización WebP queda pendiente con un enfoque diferente.

### H-8. LCP preloaded imagen incorrecta ✅

`preload` movido del segundo hero (`bg_mb6eok.png`) al primero y real candidato LCP (`blue_y3u5at.jpg`). Añadido `fetchpriority="high"`.

### H-9. No hay preconnect a Cloudinary ✅

Añadidos `<link rel="preconnect">` y `<link rel="dns-prefetch">` para `res.cloudinary.com` en `app.vue`.

### H-10. /ser-mason tiene ~80 palabras ⏳

Requiere 400–600 palabras explicando requisitos de membresía, proceso de ingreso, y qué hace la logia en Chihuahua. Opcionalmente una sección FAQ.

### H-11. No hay teléfono de contacto ⏳

Requiere un número de teléfono para publicar en footer, schema `telephone`, y /ser-mason.

### H-12. Nombre del lodge inconsistente en 3 formatos ✅

H1 del homepage corregido de `No.107` a `N.° 107`. El `site.name` en `nuxt.config.ts` también unificado. Quedan ocurrencias menores en contenido comentado de /history.

---

## Issues Medium Priority

### M-1. HSTS + security headers ✅ (parcial)

Creado `netlify.toml` con:

- `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`
- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: geolocation=(), microphone=(), camera=()`

**Pendiente:** Enviar dominio a [hstspreload.org](https://hstspreload.org) para inclusión en la lista de preload de los navegadores.

### M-2. Cache-Control HTML `no-cache` ✅

`netlify.toml` agrega `Cache-Control: public, s-maxage=300, stale-while-revalidate=60` para páginas HTML y mantiene `max-age=31536000, immutable` para `/_nuxt/*`.

> **Nota:** La regla de redirect trailing slash (`/:path*/`) fue eliminada — con `force=true` causaba `ERR_TOO_MANY_REDIRECTS` en todos los assets estáticos (`.jpeg`, `.svg`, `.png`, favicon).

### M-3. Trailing slash → duplicado de contenido ⚠️ pendiente

El redirect fue eliminado por causar loops infinitos. Alternativa segura: configurar en `nuxt.config.ts`:

```ts
nitro: {
  routeRules: { '/**': { redirect: { trailingSlash: false } } }
}
```

### M-4. BreadcrumbList JSON-LD en páginas interiores ✅

Añadido `BreadcrumbList` reactivo al locale en 6 páginas: `/history`, `/moral-code`, `/message`, `/ser-mason`, `/gallery`, `/credits`.

### M-5. `item-galery` sin traducir en menú ✅

- EN: `"Galeria"` → `"Gallery"`
- ES: `"Galeria"` → `"Galería"` (acento corregido)

### M-6. `lang` attr no reactivo en `app.vue` ✅

`htmlAttrs.lang` ahora usa `locale` (ref reactivo de `useI18n()`). Cascada del fix C-3.

### M-7. og:url ausente / og:locale incorrecto ✅

`ogLocale` ahora reactivo (`es_MX` / `en_US`). `og:url` se genera automáticamente por `@nuxtjs/seo` con `site.url` configurado.

### M-8. `lang` attribute en páginas EN ✅

Resuelto por C-3/C-4.

### M-9. No hay `llms.txt` ✅

Creado `public/llms.txt` con resumen legible por IA: identidad canónica, oficiales 2025/2026, Gran Logia, URLs de páginas clave, y guía de citación.

### M-10. No hay página de contacto/ubicación ⏳

`/contacto` retorna 404. Requiere crear página con NAP completo, embed de mapa, y horario de sesiones.

### M-11. No hay Privacy Policy ⏳

El formulario de `/ser-mason` colecta nombre, email, teléfono y mensaje sin aviso de privacidad.

### M-12. `nuxt-img` para SVG en AppFooter ✅ (bug fix)

`<nuxt-img src="/Fundacion.svg">` causaba 502 en producción porque Netlify Image CDN no soporta SVG. Cambiado a `<img>` nativo.

---

## Pendientes de acción externa (no código)

| Acción                                                 | Impacto              | Esfuerzo |
| ------------------------------------------------------ | -------------------- | -------- |
| Crear/verificar Google Business Profile                | Alto — Map Pack      | 1–2 hrs  |
| Enviar dominio a hstspreload.org                       | Medio — seguridad    | 5 min    |
| Publicar el listing en directorio de Gran Logia Cosmos | Medio — citations    | 30 min   |
| Crear entrada en Wikidata para la logia                | Medio — AI citations | 1 hr     |
| Subir video introductorio a YouTube                    | Bajo-Medio — GEO     | Variable |

---

## Pendientes de contenido (requieren texto del equipo)

| Página                     | Qué falta                                                   | Palabras mínimas |
| -------------------------- | ----------------------------------------------------------- | ---------------- |
| `/ser-mason`               | Requisitos de ingreso, proceso, FAQ                         | 400–600          |
| `i18n/locales/en.json`     | Traducción humana del código moral (ítems 7, 8, 11, 14, 15) | —                |
| Footer / Schema            | Dirección física del salón de sesiones                      | —                |
| Footer / Schema            | Teléfono de contacto                                        | —                |
| Nueva página `/contacto`   | NAP completo + embed de mapa                                | 150–300          |
| Nueva página `/privacidad` | Aviso de privacidad para el formulario                      | —                |

---

## Commits aplicados

| Commit    | Descripción                                                                                      |
| --------- | ------------------------------------------------------------------------------------------------ |
| `b1e0220` | fix(seo): critical + high-priority fixes (robots, sitemap, i18n, audio, schema, LCP, preconnect) |
| `e196c50` | fix(seo): medium-priority fixes (netlify.toml, llms.txt, BreadcrumbList, i18n galería, credits)  |
| `9620e8c` | fix: revert broken image URLs — Fundacion.svg a img nativo, menú a rutas estáticas               |
| `37ca15c` | fix(netlify): remove trailing-slash redirect — force=true causaba ERR_TOO_MANY_REDIRECTS         |
